{
  "name": "haultra-app",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor native wrapper (iOS + Android) around the live HAULTRA AI Dispatch web app at https://haultra-systems.com. This is a remote-URL wrapper, not a bundled web build — the app always loads the live site, so web deploys update every installed app instantly with no store resubmission.",
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:open:ios": "npx cap open ios",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
